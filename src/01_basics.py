"""
=============================================================================
📘 บทที่ 1: พื้นฐาน Python (Python Basics)
=============================================================================
เนื้อหา: Variables, Data Types, Operators, Input/Output
=============================================================================
"""

# =============================================================================
# 1.1 Variables (ตัวแปร)
# =============================================================================
# ตัวแปรใน Python ไม่จำเป็นต้องประกาศชนิดข้อมูล (Dynamic Typing)

# การกำหนดค่าตัวแปร
name = "สมชาย"           # String (ข้อความ)
age = 25                  # Integer (จำนวนเต็ม)
height = 175.5            # Float (ทศนิยม)
is_student = True         # Boolean (ค่าความจริง)
nothing = None            # NoneType (ไม่มีค่า)

# การกำหนดค่าหลายตัวแปรพร้อมกัน
x, y, z = 1, 2, 3
a = b = c = 0

# กฎการตั้งชื่อตัวแปร
my_variable = "valid"        # ✅ ใช้ underscore
myVariable = "valid"         # ✅ camelCase
MyVariable = "valid"         # ✅ PascalCase (มักใช้กับ Class)
_private = "valid"           # ✅ ขึ้นต้นด้วย underscore
__dunder__ = "valid"         # ✅ dunder (double underscore)
# 1variable = "invalid"      # ❌ ขึ้นต้นด้วยตัวเลขไม่ได้
# my-variable = "invalid"    # ❌ ใช้ขีดกลางไม่ได้

# =============================================================================
# 1.2 Data Types (ชนิดข้อมูล)
# =============================================================================

# --- Numeric Types (ตัวเลข) ---
integer_num = 42                    # int - จำนวนเต็ม
float_num = 3.14159                 # float - ทศนิยม
complex_num = 3 + 4j                # complex - จำนวนเชิงซ้อน
binary_num = 0b1010                 # binary - เลขฐาน 2 (= 10)
octal_num = 0o17                    # octal - เลขฐาน 8 (= 15)
hex_num = 0xFF                      # hexadecimal - เลขฐาน 16 (= 255)
big_num = 1_000_000                 # ใช้ _ คั่นหลักได้ (= 1000000)

# --- String Types (ข้อความ) ---
single_quote = 'Hello'              # Single quotes
double_quote = "World"              # Double quotes
multi_line = """This is
a multi-line
string"""                           # Triple quotes
raw_string = r"Path\to\file"        # Raw string (ไม่แปล escape)
formatted = f"Name: {name}"         # f-string (formatted string)

# String Operations
text = "Python Programming"
print(text.upper())                 # PYTHON PROGRAMMING
print(text.lower())                 # python programming
print(text.split())                 # ['Python', 'Programming']
print(text.replace("Python", "Java"))  # Java Programming
print(text[0:6])                    # Python (slicing)
print(len(text))                    # 18 (ความยาว)

# String Formatting Methods
name = "John"
age = 30
# วิธีที่ 1: % formatting (แบบเก่า)
msg1 = "Name: %s, Age: %d" % (name, age)
# วิธีที่ 2: .format()
msg2 = "Name: {}, Age: {}".format(name, age)
# วิธีที่ 3: f-string (แนะนำ - Python 3.6+)
msg3 = f"Name: {name}, Age: {age}"

# --- Boolean Types (ค่าความจริง) ---
is_true = True
is_false = False

# ค่าที่เป็น False (Falsy values)
# False, None, 0, 0.0, "", [], {}, set(), ()

# ค่าที่เป็น True (Truthy values)
# ค่าอื่นๆ ทั้งหมด

# --- Type Checking & Conversion ---
print(type(42))           # <class 'int'>
print(type(3.14))         # <class 'float'>
print(type("hello"))      # <class 'str'>

# Type Conversion
str_num = "123"
int_num = int(str_num)    # "123" -> 123
float_num = float(str_num)  # "123" -> 123.0
str_back = str(int_num)   # 123 -> "123"

# isinstance() - ตรวจสอบชนิดข้อมูล
print(isinstance(42, int))         # True
print(isinstance("hi", str))       # True
print(isinstance(42, (int, float)))  # True (เช็คหลายชนิด)

# =============================================================================
# 1.3 Operators (ตัวดำเนินการ)
# =============================================================================

# --- Arithmetic Operators (ตัวดำเนินการทางคณิตศาสตร์) ---
print(10 + 3)    # 13  (บวก)
print(10 - 3)    # 7   (ลบ)
print(10 * 3)    # 30  (คูณ)
print(10 / 3)    # 3.333... (หาร - ได้ float)
print(10 // 3)   # 3   (หารปัดเศษลง)
print(10 % 3)    # 1   (หารเอาเศษ)
print(10 ** 3)   # 1000 (ยกกำลัง)

# --- Comparison Operators (ตัวดำเนินการเปรียบเทียบ) ---
print(5 == 5)    # True  (เท่ากับ)
print(5 != 3)    # True  (ไม่เท่ากับ)
print(5 > 3)     # True  (มากกว่า)
print(5 < 3)     # False (น้อยกว่า)
print(5 >= 5)    # True  (มากกว่าหรือเท่ากับ)
print(5 <= 3)    # False (น้อยกว่าหรือเท่ากับ)

# --- Logical Operators (ตัวดำเนินการทางตรรกะ) ---
print(True and False)   # False
print(True or False)    # True
print(not True)         # False

# Short-circuit evaluation
result = True or print("Not printed")  # print ไม่ถูกเรียก
result = False and print("Not printed")  # print ไม่ถูกเรียก

# --- Assignment Operators (ตัวดำเนินการกำหนดค่า) ---
x = 10
x += 5    # x = x + 5 = 15
x -= 3    # x = x - 3 = 12
x *= 2    # x = x * 2 = 24
x /= 4    # x = x / 4 = 6.0
x //= 2   # x = x // 2 = 3.0
x **= 2   # x = x ** 2 = 9.0
x %= 4    # x = x % 4 = 1.0

# --- Bitwise Operators (ตัวดำเนินการระดับบิต) ---
a, b = 5, 3  # 5 = 0101, 3 = 0011
print(a & b)   # 1  (AND: 0001)
print(a | b)   # 7  (OR: 0111)
print(a ^ b)   # 6  (XOR: 0110)
print(~a)      # -6 (NOT: กลับบิต)
print(a << 1)  # 10 (Left shift: 1010)
print(a >> 1)  # 2  (Right shift: 0010)

# --- Identity Operators (ตัวดำเนินการเอกลักษณ์) ---
list1 = [1, 2, 3]
list2 = [1, 2, 3]
list3 = list1

print(list1 == list2)   # True  (ค่าเท่ากัน)
print(list1 is list2)   # False (ไม่ใช่ object เดียวกัน)
print(list1 is list3)   # True  (เป็น object เดียวกัน)
print(list1 is not list2)  # True

# --- Membership Operators (ตัวดำเนินการสมาชิก) ---
fruits = ["apple", "banana", "cherry"]
print("apple" in fruits)      # True
print("grape" in fruits)      # False
print("grape" not in fruits)  # True

# =============================================================================
# 1.4 Input/Output (รับข้อมูล/แสดงผล)
# =============================================================================

# --- Output with print() ---
print("Hello, World!")                    # พื้นฐาน
print("Hello", "World", sep=", ")         # กำหนดตัวคั่น
print("Hello", end=" ")                   # ไม่ขึ้นบรรทัดใหม่
print("World")
print("Tab\there")                        # Tab
print("New\nLine")                        # ขึ้นบรรทัดใหม่

# Print formatting
name = "Alice"
score = 95.5
print(f"Name: {name}, Score: {score}")           # f-string
print(f"Score: {score:.2f}")                     # 2 ทศนิยม
print(f"Score: {score:10.2f}")                   # ความกว้าง 10
print(f"Number: {42:05d}")                       # เติม 0 ข้างหน้า (00042)
print(f"Binary: {42:b}, Hex: {42:x}")           # แปลงเลขฐาน

# --- Input with input() ---
# name = input("Enter your name: ")             # รับข้อความ
# age = int(input("Enter your age: "))          # รับตัวเลข (ต้องแปลง)
# height = float(input("Enter height: "))       # รับทศนิยม

# ตัวอย่างการใช้งาน (commented เพราะต้องรอ input)
"""
name = input("ชื่อของคุณคือ: ")
age = int(input("อายุของคุณคือ: "))
print(f"สวัสดี {name}! คุณอายุ {age} ปี")
"""

# =============================================================================
# 1.5 Comments (ความคิดเห็น)
# =============================================================================

# Single-line comment
# นี่คือ comment บรรทัดเดียว

"""
Multi-line comment
สามารถเขียนได้หลายบรรทัด
ใช้ triple quotes
"""

'''
อีกแบบหนึ่งของ multi-line comment
ใช้ single quotes 3 ตัว
'''

# Docstring - ใช้อธิบาย function/class/module
def my_function():
    """
    นี่คือ docstring
    ใช้อธิบายว่า function นี้ทำอะไร
    
    Args:
        None
    
    Returns:
        None
    """
    pass

# =============================================================================
# 1.6 ตัวอย่างโปรแกรม
# =============================================================================

def calculator_example():
    """ตัวอย่างเครื่องคิดเลขอย่างง่าย"""
    print("=== เครื่องคิดเลขอย่างง่าย ===")
    
    # กำหนดค่าตัวอย่าง (ในการใช้งานจริงใช้ input())
    num1 = 10
    num2 = 3
    
    print(f"ตัวเลขที่ 1: {num1}")
    print(f"ตัวเลขที่ 2: {num2}")
    print(f"บวก: {num1} + {num2} = {num1 + num2}")
    print(f"ลบ: {num1} - {num2} = {num1 - num2}")
    print(f"คูณ: {num1} * {num2} = {num1 * num2}")
    print(f"หาร: {num1} / {num2} = {num1 / num2:.2f}")
    print(f"หารเอาเศษ: {num1} % {num2} = {num1 % num2}")
    print(f"ยกกำลัง: {num1} ** {num2} = {num1 ** num2}")

# เรียกใช้งาน
if __name__ == "__main__":
    calculator_example()
    
    print("\n" + "="*50)
    print("📘 บทที่ 1: พื้นฐาน Python - เสร็จสมบูรณ์!")
    print("="*50)
